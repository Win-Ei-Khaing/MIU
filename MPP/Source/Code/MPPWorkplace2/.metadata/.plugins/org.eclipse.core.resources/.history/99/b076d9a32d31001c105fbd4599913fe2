package Final.prob2.exam.partC;

import java.util.Comparator;

public class WinComparator implements Comparator<Long>{

	@Override
	public static int compare(Long o1, Long o2) {
		return o1.compareTo(o2);
	}

}
